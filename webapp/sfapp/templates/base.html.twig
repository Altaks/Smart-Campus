<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link href="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.1.1/flowbite.min.css" rel="stylesheet" />

        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
        <script src="https://cdn.jsdelivr.net/npm/apexcharts"></script>
        <script src="https://unpkg.com/boxicons@2.1.4/dist/boxicons.js"></script>
    </head>
    <body>

    <nav class="bg-white dark:bg-gray-900 w-full z-20 top-0 start-0 border-b border-gray-200 dark:border-gray-600">
        <div class="flex flex-row justify-between">
                <a href="/connexion" class="flex flex-wrap items-center justify-content mx-auto p-4">
                    <img src="/images/logo-univ.webp" class="h-16" alt="Logo Université de la Rochelle">
                    <span class="ml-8 self-center text-2xl font-semibold whitespace-nowrap dark:text-white">Smart Campus</span>
                </a>
            <div class="max-w-screen-xl flex flex-wrap items-center justify-content mx-auto p-4">
                {% if nav_links is defined %}
                    {% for link, label in nav_links %}
                        <a href="{{ link }}" class="mx-5 flex flex-row justify-center items-center gap-2 hover:underline">{{ label | raw }}</a>
                    {% endfor %}
                {% endif %}
            </div>
        </div>
    </nav>
    {% block body %}{% endblock %}
    <script src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.1.1/flowbite.min.js"></script>
    </body>
</html>
